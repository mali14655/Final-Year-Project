# Development: leave unset (or empty) so requests go through the CRA proxy on :3000.
# That keeps auth cookies on the same origin as the React app.
# REACT_APP_API_BASE_URL=

# Production: point at your deployed backend (must match FRONTEND_ORIGIN on the server)
# REACT_APP_API_BASE_URL=https://your-api.example.com
